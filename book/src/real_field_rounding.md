# Rounding

<!-- toc -->

## `floor` (round down)

See [`linalg_traits::RealField::floor`](https://docs.rs/linalg-traits/latest/linalg_traits/trait.RealField.html#method.floor).

```rust
{{#include ../../examples/real_field_floor.rs}}
```

## `ceil` (round up)

See [`linalg_traits::RealField::ceil`](https://docs.rs/linalg-traits/latest/linalg_traits/trait.RealField.html#method.ceil).

```rust
{{#include ../../examples/real_field_ceil.rs}}
```

## `round` (round to nearest)

See [`linalg_traits::RealField::round`](https://docs.rs/linalg-traits/latest/linalg_traits/trait.RealField.html#method.round).

```rust
{{#include ../../examples/real_field_round.rs}}
```

## `trunc` (truncate towards zero)

See [`linalg_traits::RealField::trunc`](https://docs.rs/linalg-traits/latest/linalg_traits/trait.RealField.html#method.trunc).

```rust
{{#include ../../examples/real_field_trunc.rs}}
```

## `fract` (fractional part)

See [`linalg_traits::RealField::fract`](https://docs.rs/linalg-traits/latest/linalg_traits/trait.RealField.html#method.fract).

```rust
{{#include ../../examples/real_field_fract.rs}}
```

# Zero and One

<!-- toc -->

## `zero` (additive identity)

See [`linalg_traits::RealField::zero`](https://docs.rs/linalg-traits/latest/linalg_traits/trait.RealField.html#method.zero).

```rust
{{#include ../../examples/real_field_zero.rs}}
```

## `one` (multiplicative identity)

See [`linalg_traits::RealField::one`](https://docs.rs/linalg-traits/latest/linalg_traits/trait.RealField.html#method.one).

```rust
{{#include ../../examples/real_field_one.rs}}
```

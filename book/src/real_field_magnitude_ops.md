# Magnitude-Specific Operations

<!-- toc -->

## `copysign` (copy the sign of another number)

See [`linalg_traits::RealField::copysign`](https://docs.rs/linalg-traits/latest/linalg_traits/trait.RealField.html#method.copysign).

```rust
{{#include ../../examples/real_field_copysign.rs}}
```

## `min` (minimum, ignoring NaN)

See [`linalg_traits::RealField::min`](https://docs.rs/linalg-traits/latest/linalg_traits/trait.RealField.html#method.min).

```rust
{{#include ../../examples/real_field_min.rs}}
```

## `max` (maximum, ignoring NaN)

See [`linalg_traits::RealField::max`](https://docs.rs/linalg-traits/latest/linalg_traits/trait.RealField.html#method.max).

```rust
{{#include ../../examples/real_field_max.rs}}
```

## `clamp` (restrict to an interval)

See [`linalg_traits::RealField::clamp`](https://docs.rs/linalg-traits/latest/linalg_traits/trait.RealField.html#method.clamp).

```rust
{{#include ../../examples/real_field_clamp.rs}}
```
